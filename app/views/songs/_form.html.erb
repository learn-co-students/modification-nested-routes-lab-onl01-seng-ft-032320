<%= form_for @song  do |f| %>
  <!-- passing :artist_id to the update action in the controller -->
  <%= artist_id_field(@song)%>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.submit %>
<% end %>
